services:
  web:
    build: .
    image: freudes0/pirostagram:latest
    container_name: pirostagram-web
    ports:
      - "8000:8000"
    volumes:
      - ./media:/app/media
      - ./db.sqlite3:/app/db.sqlite3